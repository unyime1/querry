import { Theme } from "../theme.slint";



export component RequestsSection inherits Rectangle {
    padding: 20px;

    Text {
        text: "Welcome to Requests";
        font-size: Theme.label_font_size_medium;
        color: Theme.label_text_color;
        horizontal-alignment: center;
    }
}